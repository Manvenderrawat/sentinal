name: Deploy Content to loganalytic-eus-sentinal-wk [cd8a25d9-744b-4bd7-8f0c-0b0202ca7f2f]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-cd8a25d9-744b-4bd7-8f0c-0b0202ca7f2f.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ main ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-cd8a25d9-744b-4bd7-8f0c-0b0202ca7f2f.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'rg-eastus-sen-analyt-wk'
      workspaceName: 'loganalytic-eus-sentinal-wk'
      workspaceId: '34149a60-669c-4520-a9f2-81ff0ea41ac7'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'AnalyticsRule,AutomationRule,HuntingQuery,Parser,Playbook,Workbook'
      branch: 'main'
      sourceControlId: 'cd8a25d9-744b-4bd7-8f0c-0b0202ca7f2f'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_cd8a25d9744b4bd78f0c0b0202ca7f2f }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-cd8a25d9-744b-4bd7-8f0c-0b0202ca7f2f.ps1